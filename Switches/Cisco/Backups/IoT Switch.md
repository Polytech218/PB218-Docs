# Notes
- Backed up via `scp -oHostKeyAlgorithms=+ssh-rsa superadmin@iot-switch.pb218.lab:config.text iot-switch.text`
- Prerequisite configurations required:
	- Level 15 privileged user (in this case superadmin)
	- `IOT-SWITCH(config)#ip scp server enable`
# To-Do
- Automate saving 